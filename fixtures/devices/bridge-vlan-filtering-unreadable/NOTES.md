# Bridge identity retained with unreadable VLAN-filtering state

Represents a successful generic bridge identity/address read with the optional sysfs VLAN-filtering attribute unavailable. The bridge collection remains usable and carries an attributed coverage note.
